<!-- 跨域用户旅程模板 → 落 CONFIG 解析的 <long_term_root>/journeys/<journey>/spec.md（新项目默认 docs/product/journeys/；legacy 经 CONFIG 映射）。
     一等对象，但懒创建（发现真实跨域问题才建）。不复制各能力内容，只写跨界承诺与不变量。 -->
# Journey: <跨域用户 Job 名>

## 用户承诺（一句话）
<用户跨多个能力完成这件现实工作时，产品必须保证的体验承诺。>

## 跨界状态转换
<用户从 A 能力到 B 能力的关键状态流转；哪里最易断>

## 关键体验不变量（跨域，必须始终成立）
- <不静默吞输入>
- <不暗中丢上下文>
- <必须 fork 时操作前明说>
- <"已完成"须对应可用产物>
- <runtime 不可继续时须给原因+恢复方式>

## 依赖的能力 requirement-id
- <CAP-X-REQ-00n> · <CAP-Y-REQ-00m> …

## 第一面墙场景（/cw-experience-* 用）
- <跨域端到端 Witness 旅程描述 + 需配的 system 证据>

## 最近 Human 接受的基线
- <baselines/ 指针 + 日期>
