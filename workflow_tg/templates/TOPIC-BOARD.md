# Topic Board

> `/i-think` 与 `/cw-think` 深碰 topic 的注册表（共用 docs/topics 生态，`tbat`/`tcopy`/`tinit` 依赖此格式）。
> **本表仅可再生聚合索引、非真相**：`状态(DISCUSSING/MERGED)`/`结晶数` 的真相在各 `<topic_id>-<slug>/` 目录内 session 文件头；并发丢 entry 可 `ls` topic 目录 + 读各文件头**重建**。主流程消费 `MERGED` 结晶时**以 per-topic session 文件头为准**（别把本共享表当唯一真相）。

| topic_id | scope | 主题 | 状态 | 结晶数 | 创建时间 | 最后更新 |
|----------|-------|------|------|--------|---------|---------|
